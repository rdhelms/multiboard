(function() {
  "use strict";

  angular.module('multiboard', ['templates', 'ui.router', 'LocalStorageModule'])
        .config(function($stateProvider, $urlRouterProvider) {
          $urlRouterProvider.otherwise('/');

          $stateProvider.state('board', {
            url: '/',
            abstract: true,
            template: '<ui-view></ui-view>'
          }).state('board.scenes', {
            url: '',
            templateUrl: 'scenes.html',
            controller: 'scenesCtrl as scenes'
          }).state('board.draw', {
            url: 'draw',
            templateUrl: 'draw.html',
            controller: 'drawCtrl as draw'
          });
        });

})();
